# 开发模式的前端Dockerfile
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制package文件
COPY frontend/package*.json ./

# 安装所有依赖（包括开发依赖）
RUN npm ci

# 复制源代码
COPY frontend/ ./

# 暴露端口
EXPOSE 5173

# 开发模式启动命令
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
